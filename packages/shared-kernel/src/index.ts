/**
 * Intentionally minimal.
 *
 * Only stable, context-independent domain primitives may be exported from this
 * package. Infrastructure helpers and speculative abstractions do not belong here.
 */
export const SHARED_KERNEL_VERSION = 1 as const;
